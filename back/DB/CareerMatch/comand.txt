Запустить backend по wi-fi
npm run start:dev

Запустить frontend wi-fi
yarn dev  -- --host 127.0.0.1 --port 3000

Установить офлайн
"windows": {
	"webviewInstallMode": {
		"type": "offlineInstaller"
	}
},

npm install -g nodemon --save-dev


Linux

1. wsl

sudo apt update
sudo apt install имя_пакета 

sudo pacman -S имя_пакета
